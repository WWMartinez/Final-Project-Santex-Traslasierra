import InfoCard from "../models/infoCard.model";
import cloudinary from "../utils/cloudinary";

export const infoCard = async (req, res, next ) => {
  const { image, title, description, category } = req.body;

  try {
    
  } catch (error) {
    
  }
};